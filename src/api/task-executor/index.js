// @flow
'use strict';

const SystemExecutor = require('./system-executor');

// TODO: For now we expose the system executor. We should do that but expose it as a TaskExecutor.
module.exports = SystemExecutor;
